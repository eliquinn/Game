#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <string.h>

void input(char *str);
char opt[64];
int loc =0;

void loop()
{
  switch(loc) {
case 1:
  }}

void input(char *str)
{
printf(">");
  fgets(str, 64, stdin);
  str[strcspn[str, "\n")] = '\0';
}

int main()
{
  input(opt);
printf("You said %s", fart);
return 0;
}
